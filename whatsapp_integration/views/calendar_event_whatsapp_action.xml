<odoo>
    <record id="calendar_event_action_send_whatsapp" model="ir.actions.server">
        <field name="name">Enviar recordatorio por WhatsApp</field>
        <field name="model_id" ref="calendar.model_calendar_event"/>
        <field name="state">code</field>
        <field name="code">action = records.action_send_whatsapp_reminder()</field>
        <field name="binding_model_id" ref="calendar.model_calendar_event"/>
        <field name="binding_type">action</field>
    </record>
</odoo>
