<odoo>
    <record id="sale_order_action_send_whatsapp" model="ir.actions.server">
        <field name="name">Enviar presupuesto por WhatsApp</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code">action = records.action_send_whatsapp()</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">action</field>
    </record>
</odoo>
